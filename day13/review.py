import torch
a = torch.tensor([[2.6, 4.6],
                  [3.4, 5.8],
                  [4.5, 2.4]])

# b = torch.argmax(a,dim=-1)
# labels = torch.tensor([1, 0, 1])
# print((b==labels).sum(), end=' ')
# print(len(labels))
# import random
#
# print(random.randint(0, 1))
